/**
 * SUN PEPE Landing Page — sunpepe-landing.js
 * Scoped entirely to .sunpepe-landing.
 * Phase 6: GSAP ScrollTrigger animation will be initialised here.
 */
( function () {
    'use strict';

    var root = document.querySelector( '.sunpepe-landing' );
    if ( ! root ) { return; }

    // Phase 6: scroll animation initialisation goes here.

}() );
